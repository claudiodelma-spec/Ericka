import React, { useState } from "react";
import {
  LayoutDashboard,
  Wallet,
  Building2,
  Users2,
  Target,
  Palmtree,
  PiggyBank,
  CalendarClock,
  BarChart3,
  ShoppingBag,
  Tag,
  UtensilsCrossed,
  Images,
  FileText,
  Settings,
  UserCog,
  ShieldCheck,
  TrendingUp,
  TrendingDown,
  AlertTriangle,
  CheckCircle2,
  Info
} from "lucide-react";

export default function AdminDashboard() {
  const [activeItem, setActiveItem] = useState("Inicio");

  return (
    <div className="flex min-h-screen bg-slate-50 text-slate-800 font-sans">
      {/* BARRA LATERAL */}
      <aside className="w-64 bg-slate-900 text-slate-300 flex flex-col shrink-0">
        <div className="px-5 py-5 flex items-center gap-3 border-b border-slate-800">
          <span className="h-9 w-9 rounded-full bg-orange-400 flex items-center justify-center text-slate-900 font-bold">
            TL
          </span>
          <div>
            <p className="text-white font-semibold leading-tight">Tendita Landy</p>
            <p className="text-xs text-slate-500">Panel de Gestión</p>
          </div>
        </div>

        <nav className="flex-1 overflow-y-auto py-3 text-sm space-y-1 px-3">
          {["Inicio", "Ingresos / Ventas", "Rentas", "Nómina", "Gastos", "Metas y Ahorro", "Vacaciones", "Productos", "Pedidos", "Configuración"].map((item) => (
            <button
              key={item}
              onClick={() => setActiveItem(item)}
              className={`w-full flex items-center gap-3 px-4 py-2.5 rounded-xl transition-colors ${
                activeItem === item ? "bg-blue-600 text-white font-medium" : "hover:bg-slate-800 text-slate-400"
              }`}
            >
              {item}
            </button>
          ))}
        </nav>
      </aside>

      {/* CONTENIDO PRINCIPAL */}
      <main className="flex-1 p-8 space-y-6">
        <header className="flex justify-between items-center border-b border-slate-200 pb-4">
          <div>
            <h1 className="text-xl font-bold text-slate-900">Panel de Control</h1>
            <p className="text-xs text-slate-400">Resumen financiero y métricas globales</p>
          </div>
          <span className="text-xs bg-amber-100 text-amber-700 px-3 py-1 rounded-full font-bold">
            🧪 MODO DEMO / SANDBOX
          </span>
        </header>

        {/* MÉTRICAS PRINCIPALES */}
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
          <div className="bg-white p-5 rounded-2xl border border-slate-200 shadow-sm">
            <p className="text-xs text-slate-400">Ventas del Día</p>
            <p className="text-2xl font-black text-slate-900 mt-1">$5,240</p>
          </div>
          <div className="bg-white p-5 rounded-2xl border border-slate-200 shadow-sm">
            <p className="text-xs text-slate-400">Ganancia Neta</p>
            <p className="text-2xl font-black text-emerald-600 mt-1">$2,480</p>
          </div>
          <div className="bg-white p-5 rounded-2xl border border-slate-200 shadow-sm">
            <p className="text-xs text-slate-400">Gastos del Día</p>
            <p className="text-2xl font-black text-rose-600 mt-1">$2,760</p>
          </div>
          <div className="bg-white p-5 rounded-2xl border border-slate-200 shadow-sm border-l-4 border-l-blue-600">
            <p className="text-xs text-blue-600 font-bold">Ahorro Diario Necesario</p>
            <p className="text-2xl font-black text-blue-900 mt-1">$620 / día</p>
          </div>
        </div>

        {/* PRÓXIMOS PAGOS */}
        <div className="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm space-y-3">
          <h2 className="text-sm font-bold text-slate-800">Próximos Pagos Semanales</h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4 text-xs">
            <div className="p-3 bg-slate-50 rounded-xl">
              <p className="text-slate-400">Renta (Semanal)</p>
              <p className="font-bold text-slate-700 text-sm mt-0.5">$2,500</p>
            </div>
            <div className="p-3 bg-slate-50 rounded-xl">
              <p className="text-slate-400">Nómina (Semanal)</p>
              <p className="font-bold text-slate-700 text-sm mt-0.5">$1,000</p>
            </div>
            <div className="p-3 bg-slate-50 rounded-xl">
              <p className="text-slate-400">Gasto Mensual Estimado</p>
              <p className="font-bold text-slate-700 text-sm mt-0.5">$7,000</p>
            </div>
          </div>
        </div>
      </main>
    </div>
  );
}